<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The XML theme only hosts the activity window; Compose applies the app visual system at runtime. -->
    <style name="Theme.PopPost" parent="Theme.Material3.DayNight.NoActionBar" />
</resources>
